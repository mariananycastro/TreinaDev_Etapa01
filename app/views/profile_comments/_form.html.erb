  
<% if @profile_comment.errors.any? %>
  <ul>
  <% @profile_comment.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
  </ul>
<% end %>

<%= form_with model: [@profile, @profile_comment], local:true do |f| %>
    <p><%= f.label :comment, 'Comentário' %>
    <%= f.text_field :comment %></p>
    <p> <%= f.submit 'Salvar Comentário'%></p>
<% end %>

<p><%= link_to 'Home', root_path %> </p>
<p><%= link_to 'Nova Vaga', new_headhunter_job_opportunity_path(current_headhunter) %> </p>
<p><%= link_to 'Vagas Cadastradas', headhunter_job_opportunities_path(current_headhunter) %></p>
<p> <%= link_to 'Pesquisar Candidatos', search_profiles_path %>
<p><%= link_to 'Voltar', :back %></p>